import UIKit

/// If I had more time I would've used SwiftGen to generate these https://github.com/SwiftGen/SwiftGen
struct Images {
    static let productPlaceholder = UIImage(named: "productPlaceholder")
}
